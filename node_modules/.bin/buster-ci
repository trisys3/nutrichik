#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../buster/bin/buster-ci" "$@"
  ret=$?
else 
  node  "$basedir/../buster/bin/buster-ci" "$@"
  ret=$?
fi
exit $ret
