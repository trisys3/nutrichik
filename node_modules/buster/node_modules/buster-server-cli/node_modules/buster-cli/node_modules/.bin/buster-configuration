#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../buster-configuration/bin/buster-configuration" "$@"
  ret=$?
else 
  node  "$basedir/../buster-configuration/bin/buster-configuration" "$@"
  ret=$?
fi
exit $ret
